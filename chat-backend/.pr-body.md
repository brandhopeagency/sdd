## Summary
- classify session-create `userId` intent before guest gating
- return `401 UNAUTHORIZED` for UUID/non-guest unauthenticated requests to trigger refresh flow
- keep guest-mode enforcement for `guest_*` requests and add regression route tests

## Test plan
- [x] npm test -- tests/unit/chat.sessions.route.test.ts
- [x] npm run typecheck
