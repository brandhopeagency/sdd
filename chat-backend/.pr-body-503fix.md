## Summary
- fix migration 015 failure on environments with legacy `session_tags(tag_id)` schema from migration 005
- add idempotent compatibility block to migrate legacy columns/data toward `tag_definition_id` shape
- prevent startup migration exceptions that were causing `/api/review/queue` to return 503

## Test plan
- [x] npm run typecheck
- [ ] verify dev logs after deploy show review queue no longer 503
